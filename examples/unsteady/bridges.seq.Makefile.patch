--- origin/Makefile	2020-08-23 03:12:30.321739277 -0400
+++ bridges.seq.Makefile	2020-08-23 03:24:05.000000000 -0400
@@ -48,10 +48,10 @@ serialCXX    = icc
 # Compiler to use with MPI parallelism
 parallelCXX  = mpiicc
 # General compiler flags (e.g. -Wall to turn on all warnings on g++)
-compileFlags = -Wall -Wextra -Wnon-virtual-dtor -Wno-deprecated-declarations -DPLB_MAC_OS_X -std=gnu99
+compileFlags = -Wall -Wextra -Wnon-virtual-dtor -Wno-deprecated-declarations -DPLB_MAC_OS_X -std=gnu99 -xCORE-AVX2
 
 # General linker flags (don't put library includes into this flag)
-linkFlags    = -lm
+linkFlags    = -lm -xCORE-AVX2
 # Compiler flags to use when optimization mode is on
 optimFlags   = -O3
 #optimFlags   = -xHOST -O3 -ip -no-prec-div -static
